//! `read_page` -- accessibility-tree representation with element ref IDs. **Observe** tier.
//!
//! Produced by the extension's content script (per the Phase 0 decision to keep DOM reads in a
//! content script for output-format fidelity). The binary forwards the request and relays the
//! result. Implemented in Phase 2.
