{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The bundled Ghostlight workbench can invoke only registered application commands.",
  "windows": ["main"],
  "permissions": []
}
