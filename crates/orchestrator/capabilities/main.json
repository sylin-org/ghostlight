{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The bundled Ghostlight workbench can invoke only registered application commands, listen for orchestrator-published changes, and read the native notification permission required by the notification plugin bootstrap. It cannot emit events or send notifications of its own.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "notification:allow-is-permission-granted"
  ]
}
