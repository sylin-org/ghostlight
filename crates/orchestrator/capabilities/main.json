{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The bundled Ghostlight workbench can invoke only registered application commands and listen for orchestrator-published changes. It cannot emit events of its own.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
