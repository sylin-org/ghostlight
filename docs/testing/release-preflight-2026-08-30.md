# Release preflight -- 1.3.0 release candidate (frozen revision dfcf8c4a)

```text
date_utc: 2026-08-30T20:25:15Z
runner_head: 7c1e3c9c7834775a0fe8504b776bc8b5e4ea9f73 (docs-only descendant of the frozen revision)
source_revision: dfcf8c4a5d5881ab2d9fc6ded8b99030b0435be0 (docs/release/freeze.json)
tree_dirty: false
toolchain: rustc 1.95.0 (59807616e 2026-04-14); node v24.7.0
host: Microsoft Windows 10.0.26200
```

Windows half of the G1 source gate, generated by `scripts/release-preflight.ps1` with
`-IncludeDependencyGates` against a fresh custom target (`.target-preflight-130`). This line
changed no extension byte, so the store adapter 1.1.0 submission continues to cover the
candidate.

## Stage results

| Stage | Result | Detail |
| --- | --- | --- |
| cargo fmt --all -- --check | PASS | clean |
| cargo clippy --workspace --all-targets -D warnings | PASS | workspace clean, warnings denied |
| cargo test --workspace | PASS | every suite green, 0 failed across all 10 result lines |
| extension tests (npm test) | PASS | 156/156 |
| npm launcher tests (packaging/npm) | PASS | 10/10 |
| MCPB launcher tests | PASS | 4/4 |
| shell script syntax (sh -n scripts/*.sh) | PASS |  |
| isolated workspace build (.target-preflight-130) | PASS | fresh debug build of the frozen tree |
| process journey | PASS | reconnect -> open/read/find/flow(execute/article/tree/wheel/upload/drop/guarded) -> screenshot/region/chain -> recording -> close -> pinned no-adapter refusal |
| CLI journey | PASS | demand-free call -> governed result -> cli-attributed audit -> batch session -> channel refusal; the no-browser pin accepts the closed honest set |
| CLI PowerShell journey | PASS | separate processes, one session, open/list/read/capture/close |
| workbench surface | PASS | a broken panel costs its own panel and nothing else |
| policy grammar | PASS | readback matches matcher behavior |
| capability matrix (behavior evidence map) | PASS | 21 COMPLETE rows, 4 SUPERSEDED rows, all evidenced |
| JavaScript syntax (ui/app.js, preview server) | PASS | node --check clean |
| freeze binding (docs/release/freeze.json) | PASS | HEAD is a docs-only descendant of dfcf8c4a; product paths identical |
| dependency gates (deny licenses/bans/sources + audit) | PASS | the authoritative deny-plus-audit split: bans, licenses, and sources ok; cargo audit exits zero with exactly the 17 documented GTK/Tauri-chain allowances |

Stages: 17 passed, 0 failed, 0 skipped.

## Fringe-stability review (manual row)

Diff `765df478..dfcf8c4a` (the 1.2.0 candidate revision through the 1.3.0 frozen revision)
across the stable fringe is empty: `crates/bridge`, `crates/mcp-connector`,
`crates/browser-connector`, and `extension/` carry no change on this whole line. The line's
product work lives in the orchestrator (harness setup, Devin registry, recovery language),
the bundled workbench UI, docs, and tests; the extension remains policy-free and
byte-identical to the approved store revision.

## Rows completed on this host around the runner

Against the release-preparation tree at source version 1.3.0:

- `scripts/check-public-surfaces.ps1` (offline): source 1.3.0; public 1.2.0; public adapter
  1.0.0, as intended before publication.
- `scripts/check-repository-integrity.ps1`: 884 tracked files readable; local links valid;
  source version 1.3.0 aligned; historical ASCII exceptions fixed at 26 named files with no
  new exception; every extension manifest permission has exactly one store justification;
  capability matrix green.
- `npm test --prefix packaging/npm` and the MCPB launcher suite re-ran green after the bump
  (10/10 and 4/4), and the CLI and process journeys re-ran green against freshly rebuilt
  1.3.0 debug binaries.
- `scripts/check-release-access.ps1 -Online` (read-only pre-candidate inspection):
  GITHUB_AUTH valid, NPM_AUTH valid, CHROME_WEB_STORE v2-item-valid; the five optional Chrome
  API values and the two MCP Registry values present as names-and-states only. No credential
  value was read into any output.

## Rows outside this runner

| Row | Where it closes |
| --- | --- |
| Full CI matrix on the frozen revision's docs-only descendant | GitHub Actions run 33333480506, green on 7c1e3c9c |
| Clean Windows/Linux install, upgrade, uninstall | release-environment machines, owner-run |

The G2 candidate build dispatches at the exact frozen revision `dfcf8c4a`, so the candidate
binds this revision rather than the docs-only descendant.
